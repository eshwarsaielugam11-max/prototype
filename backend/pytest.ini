[pytest]
pythonpath = . ..
testpaths = tests ../ml/tests
markers =
    ollama: marks tests requiring active local Ollama daemon (deselect with '-m "not ollama"')
    slow: marks tests requiring heavy model backbone loading
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
